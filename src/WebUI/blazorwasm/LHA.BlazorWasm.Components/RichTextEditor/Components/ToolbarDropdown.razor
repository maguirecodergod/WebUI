@namespace LHA.BlazorWasm.Components.RichTextEditor.Components

<div class="rte-dropdown-wrapper" @onclick:stopPropagation>
    @if (!string.IsNullOrEmpty(Tooltip))
    {
        <LHA.BlazorWasm.Components.Tooltip.Tooltip Text="@Tooltip" Style="display: inline-flex;">
            <LHAButton Class="@GetButtonCssClass()" Style="CButtonStyle.Link" Disabled="@Disabled" OnClick="ToggleDropdown">
                @if (!string.IsNullOrEmpty(SvgIcon))
                {
                    @((MarkupString)SvgIcon)
                }
                <i class="bi bi-chevron-down rte-dropdown-arrow"></i>
            </LHAButton>
        </LHA.BlazorWasm.Components.Tooltip.Tooltip>
    }
    else
    {
        <LHAButton Class="@GetButtonCssClass()" Style="CButtonStyle.Link" Disabled="@Disabled" OnClick="ToggleDropdown">
            @if (!string.IsNullOrEmpty(SvgIcon))
            {
                @((MarkupString)SvgIcon)
            }
            <i class="bi bi-chevron-down rte-dropdown-arrow"></i>
        </LHAButton>
    }

    @if (IsOpen)
    {
        <div class="rte-dropdown-popup @(Alignment.ToString().ToLowerInvariant()) open" @onclick:stopPropagation>
            @ChildContent
        </div>
    }
</div>
