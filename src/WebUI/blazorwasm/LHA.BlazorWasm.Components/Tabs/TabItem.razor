@* ═══════════════════════════════════════════════════════════════════
   TabItem — Declarative child component for <Tabs>.
   Registers itself into the parent Tabs via CascadingParameter and
   exposes a <Content> render-fragment slot.
   ═══════════════════════════════════════════════════════════════════ *@
@namespace LHA.BlazorWasm.Components.Tabs
@* No markup — this component is purely semantic / registration-only.
   The parent Tabs renders the header button and content panel. *@
