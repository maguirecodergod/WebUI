apiVersion: v1
kind: Service
metadata:
  name: {{ include "lha-webui.fullname" . }}
  labels:
    {{- include "lha-webui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "lha-webui.selectorLabels" . | nindent 4 }}
